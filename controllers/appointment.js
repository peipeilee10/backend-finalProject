// import appointment from '../models/appointment.js'
// import users from '../models/users.js'

// 預約
export const appointmentcheckout = async (req, res) => {
  try {
    // const result = await appointment.create({ userId: req.user._id })
  } catch (error) {

  }
}
